

using Newtonsoft.Json;
using UnityEngine;

public static class CommonSaveData
{
    public static void SaveRuneData()
    {
        //var dataString = JsonConvert.SerializeObject(this);
        //Debug.Log("data: " + dataString);
    }
}

